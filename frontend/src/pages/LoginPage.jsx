import { useState } from 'react'
import { Link, Navigate, useLocation, useNavigate } from 'react-router-dom'
import { Lock, Visibility, VisibilityOff } from '@mui/icons-material'
import { useAuth } from '../context/AuthContext'
import { Button } from '../components/UI'
import { message } from '../utils'
export default function LoginPage() { const { login, isAuthenticated } = useAuth(); const navigate = useNavigate(); const location = useLocation(); const [form, setForm] = useState({ email:'', password:'' }); const [show, setShow] = useState(false); const [busy,setBusy] = useState(false); const [error,setError] = useState(''); if (isAuthenticated) return <Navigate to="/" replace />; const submit = async (e) => { e.preventDefault(); setBusy(true); setError(''); try { await login(form); navigate(location.state?.from?.pathname || '/') } catch (err) { setError(message(err)) } finally { setBusy(false) } }; return <div className="login"><section className="login-brand"><div className="login-logo"><Lock /></div><p className="eyebrow">Enterprise procurement</p><h1>Purchasing, made accountable.</h1><p>Manage requests, approvals, suppliers, and purchasing decisions from one secure workspace.</p><ul><li>Structured approval workflow</li><li>Supplier and compliance oversight</li><li>Live operational reporting</li></ul></section><section className="login-panel"><form className="login-form auth-card" onSubmit={submit}><div className="login-form-logo"><Lock /></div><h2>Welcome back</h2><p>Sign in to your procurement workspace.</p>{error && <div className="form-error">{error}</div>}<label>Email<input type="email" required autoComplete="email" value={form.email} onChange={(e)=>setForm({...form,email:e.target.value})} placeholder="name@company.com" /></label><label>Password<span className="password-input"><input type={show?'text':'password'} required autoComplete="current-password" value={form.password} onChange={(e)=>setForm({...form,password:e.target.value})} placeholder="Enter your password" /><button type="button" aria-label="Toggle password visibility" onClick={()=>setShow(!show)}>{show?<VisibilityOff/>:<Visibility/>}</button></span></label><p className="forgot-link"><Link to="/forgot-password">Forgot password?</Link></p><Button type="submit" disabled={busy} className="login-submit">{busy?'Signing in…':'Sign in'}</Button></form><p className="auth-switch">New to ProcureFlow? <Link to="/register">Create an employee account</Link></p></section></div> }
